import { Component,OnInit } from '@angular/core'; import { CommonModule } from '@angular/common'; import { RouterLink } from '@angular/router'; import { forkJoin } from 'rxjs'; import { ApiService } from '../core/api.service'; import { License } from '../core/models';
@Component({standalone:true,imports:[CommonModule,RouterLink],template:`<div class="hero"><div><small>OPERATIVNI PREGLED</small><h2>Infrastruktura na jednom mestu</h2><p>Pratite resurse i licence kojima je potrebna pažnja.</p></div><a routerLink="/devices" class="primary link">Pregled uređaja</a></div><div class="stats"><article><span class="icon blue">▣</span><div><b>{{deviceCount}}</b><small>Ukupno uređaja</small></div></article><article><span class="icon green">✓</span><div><b>{{activeCount}}</b><small>Aktivnih uređaja</small></div></article><article><span class="icon amber">!</span><div><b>{{expiring.length}}</b><small>Licence uskoro ističu</small></div></article><article><span class="icon red">×</span><div><b>{{expiredCount}}</b><small>Isteklih licenci</small></div></article></div><div class="panel"><div class="panel-head"><div><small>ZAHTEVA PAŽNJU</small><h3>Licence koje uskoro ističu</h3></div><a routerLink="/licenses">Sve licence →</a></div><table><thead><tr><th>Softver</th><th>Tip</th><th>Datum isteka</th><th>Status</th></tr></thead><tbody><tr *ngFor="let l of expiring"><td><b>{{l.softwareName}}</b></td><td>{{l.licenseType}}</td><td>{{l.endDate|date:'dd.MM.yyyy'}}</td><td><span class="badge warning">Ističe uskoro</span></td></tr><tr *ngIf="!expiring.length"><td colspan="4" class="empty">Nema licenci koje ističu u narednih 30 dana.</td></tr></tbody></table></div>`})
export class DashboardComponent implements OnInit {deviceCount=0;activeCount=0;expiredCount=0;expiring:License[]=[];constructor(private api:ApiService){}
ngOnInit():void{forkJoin({devices:this.api.devices(),licenses:this.api.licenses()}).subscribe(({devices,licenses})=>{this.deviceCount=devices.length;this.activeCount=devices.filter(d=>d.status==='ACTIVE').length;this.expiredCount=licenses.filter(l=>l.computedStatus==='EXPIRED').length;this.expiring=licenses.filter(l=>l.computedStatus==='EXPIRING_SOON').slice(0,5);});}}
