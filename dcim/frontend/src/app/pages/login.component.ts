import { Component } from '@angular/core'; import { CommonModule } from '@angular/common'; import { FormBuilder,ReactiveFormsModule,Validators } from '@angular/forms'; import { Router } from '@angular/router'; import { AuthService } from '../core/auth.service';
@Component({standalone:true,imports:[CommonModule,ReactiveFormsModule],template:`<div class="login-page"><div class="login-art"><div class="mark">DC</div><h1>Data centar pod kontrolom.</h1><p>Jedinstven pregled uređaja, softvera, licenci i odgovornih timova.</p><div class="grid-art"></div></div><form class="login-card" [formGroup]="form" (ngSubmit)="submit()"><small>DCIM PORTAL</small><h2>Dobro došli</h2><p>Prijavite se poslovnim nalogom.</p><label>Korisničko ime<input formControlName="username" autocomplete="username" placeholder="Unesite korisničko ime"></label><label>Lozinka<input type="password" formControlName="password" autocomplete="current-password" placeholder="••••••••"></label><div class="error" *ngIf="error">{{error}}</div><button class="primary" [disabled]="form.invalid||loading">{{loading?'Prijavljivanje...':'Prijavi se'}}</button><footer>Za pristup se obratite administratoru sistema.</footer></form></div>`})
export class LoginComponent {
 form=this.fb.nonNullable.group({username:['',[Validators.required]],password:['',[Validators.required]]}); loading=false; error='';
 constructor(private fb:FormBuilder,private auth:AuthService,private router:Router){if(auth.authenticated())void router.navigateByUrl('/');}
 submit():void{if(this.form.invalid)return;this.loading=true;this.error='';const v=this.form.getRawValue();this.auth.login(v.username,v.password).subscribe({next:()=>void this.router.navigateByUrl('/'),error:e=>{this.loading=false;this.error=e.error?.message??'Prijava nije uspela.';}});}
}
